{% extends "blogBundle:LayoutPost:layout.html.twig" %}

{% block title %}
  Lecture du post - {{ parent() }}
{% endblock %}

{% block post_body %}

  <h2>{{ advert.title }}</h2>
  <i>Par {{ advert.auteur }}, le {{ advert.date|date('d/m/Y') }}</i>

  <div class="well">
    {{ advert.body }}
  </div>

  <p>
    <a href="{{ path('blog_home') }}" class="btn btn-default">
      <i class="glyphicon glyphicon-chevron-left"></i>
      Retour à la page d'accueil
    </a>
    <a href="{{ path('blog_post_comment', {'id': advert.id}) }}" class="btn btn-default">
      <i class="glyphicon glyphicon-edit"></i>
      Commenter ce post
    </a>
    
    <!-- if admin <a href="{{ path('blog_post_edit', {'id': advert.id}) }}" class="btn btn-default">
      <i class="glyphicon glyphicon-edit"></i>
      Modifier l'annonce
    </a>
    <a href="{{ path('blog_post_delete', {'id': advert.id}) }}" class="btn btn-danger">
      <i class="glyphicon glyphicon-trash"></i>
      Supprimer l'annonce
    </a>
    -->
  </p>

{% endblock %}