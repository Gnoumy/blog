{% extends "blogBundle:LayoutPost:layout.html.twig" %}

{% block title %}
  Accueil - {{ parent() }}
{% endblock %}


{% block post_body %}

{% for advert in listAdverts %}
<div class="column col-sm-9" id="main">
  <div class="padding">
    <div class="full col-sm-9">
      <div class="row">    
        <div class="col-sm-10">
          <h3><a href="{{ path('blog_view', {'id': advert.id}) }}">
          {{ advert.title }}
        </a>
          </h3>
          <h4>
            <span class="label label-default">{{ advert.category }}</span>
          </h4>
          <h4>
            <small class="text-muted">Par : {{ advert.author }} • 
              <a href="#" class="text-muted">Lire la suite</a>
            </small>
          </h4>
        </div>
        <div class="col-sm-2">
          <a href="{{ path('blog_view', {'id': advert.id}) }}" class="pull-right"><img src="http://api.randomuser.me/portraits/thumb/men/86.jpg" class="img-circle">
          </a>
        </div> 
      </div>
      <div class="row divider">    
        <div class="col-sm-12"><hr>
        </div>
      </div>
    </div><!-- /col-9 -->
  </div><!-- /padding -->
</div>
<!-- /main -->
{% else %}
      <li>Pas (encore !) de postes</li>
{% endfor %}


{#
  <ul>
    {% for advert in listAdverts %}
      <li>
        <a href="{{ path('blog_view', {'id': advert.id}) }}">
          {{ advert.title }}
        </a>
        par {{ advert.author }},
        le {{ advert.date|date('d/m/Y') }}
      </li>
    {% else %}
      <li>Pas (encore !) de postes</li>
    {% endfor %}
  </ul>
#}
<style src="{{ asset('blogBundle/public/css/style.css') }}"/>
<script src="{{ asset('blogBundle/public/js/script.js') }}" type="text/javascript" />

{% endblock %}



